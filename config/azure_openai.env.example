# Azure OpenAI (Responses) config
# Copy this file to `config/azure_openai.env` and fill in values.
# DO NOT commit `config/azure_openai.env`.

# Example: https://<your-resource-name>.openai.azure.com
AZURE_OPENAI_ENDPOINT=

# This is your Azure OpenAI *deployment name* (Responses)
AZURE_OPENAI_RESPONSES_DEPLOYMENT_NAME=

# Optional. If you leave it empty, the SDK default will be used.
AZURE_OPENAI_API_VERSION=

# Optional. REST test-only api-version override. If set, it takes precedence in REST tests.
AZURE_OPENAI_REST_API_VERSION=

# Entra ID auth (recommended)
# If you see tenant mismatch errors, set AZURE_TENANT_ID to your resource tenant (Directory ID)
AZURE_TENANT_ID=

# API Key auth (optional). If set, the backend will use key auth instead of Entra ID.
# Note: key auth might be disabled for your resource.
AZURE_OPENAI_API_KEY=

# Agent behavior
AGENT_NAME=Assistant
AGENT_INSTRUCTIONS=You are a helpful assistant.
